# 隐私说明

Agent Record 在本机录制和处理视频，不提供账号、遥测、云端同步或自动上传。

## 处理的数据

录制启动后：

- macOS ScreenCaptureKit 捕获 CLI 指定的浏览器窗口，不采集音频和系统光标。
- Chrome 扩展记录页面网址、标题、视口、鼠标位置、点击、输入位置、滚动和页面切换事件。
- 时间轴不保存输入值、页面正文、密码、令牌、网址查询参数或片段。

页面上可见的账号、输入内容和其他信息仍可能出现在视频画面中，发布前应自行检查成片。

## 保存与删除

原始视频、时间轴、项目文件和最终 MP4 保存在项目的 `artifacts/` 目录。删除对应会话目录和成片即可删除本地数据；系统备份中的副本需要单独处理。

## 权限

扩展向普通 HTTP/HTTPS 页面注入事件采集脚本，并通过 `127.0.0.1:43127` 与本地录制服务通信。扩展不使用 Debugger、Tab Capture、Offscreen、下载或存储权限。macOS 会单独请求系统屏幕录制权限。

问题请提交到 [GitHub Issues](https://github.com/oil-oil/agent-record/issues)。
