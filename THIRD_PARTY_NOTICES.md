# 第三方说明

Agent Record 代码采用 AGPL-3.0-only。依赖的准确版本以 `package-lock.json` 和 `website/package-lock.json` 为准。

- React、Vite、Tailwind CSS、Radix UI、Lucide、TypeScript、Next.js 等依赖遵循各自许可证。
- Remotion 4 使用其自定义许可证。个人、非营利组织和不超过三人的公司可按免费许可使用；其他商业组织需要向 Remotion 获取许可。
- FFmpeg/ffprobe 是用户单独安装的外部工具，不包含在发布包中。
- Chrome 和 macOS ScreenCaptureKit 由对应平台提供。
- Agent Record 的 Logo、图标和内置 `refined-*` 背景由项目自行制作；品牌使用边界见 `TRADEMARKS.md`。

发布包不包含历史研究素材、第三方网页截图或测试录屏。
